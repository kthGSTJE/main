#include<stdio.h>
#include<stdlib.h>
#include<time.h>
#include<string.h>

#define SIZE 10

void bubbleSort(char charArray[][SIZE], int size);


int main (void){
    char teams[SIZE][SIZE] = {"DIF", "AIK", "HIF", "GAIS", "MALMÖ", "AALAGET"};

}

void bubbleSort(char teamArray[][SIZE], int size){
    char tempArray[SIZE];
    for (int i = 0; i < size; i++)
    {
        for (int j = 0; j < SIZE; j++)
        {
            if (teamArray)
            {
                /* code */
            }
            
        }
        
    }
    
}